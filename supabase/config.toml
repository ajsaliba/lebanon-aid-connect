project_id = "qmakvcizkocctnojbskf"

# ── Auth (local dev only — hosted project settings live in Supabase Dashboard) ─
[auth]
enable_signup = true
# Minimum password length enforced server-side
minimum_password_length = 6

[auth.email]
# Require users to confirm their email address before signing in.
# For the HOSTED project, enable this at:
#   Supabase Dashboard → Authentication → Providers → Email → "Confirm email"
enable_confirmations = true
double_confirm_changes = true
enable_signup = true
# ─────────────────────────────────────────────────────────────────────────────

[functions.rss-news-feed]
verify_jwt = false

[functions.classify-threat]
verify_jwt = false

[functions.world-brief]
verify_jwt = false

[functions.sentiment-velocity]
verify_jwt = false

[functions.geocode]
verify_jwt = false